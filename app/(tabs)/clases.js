import { View, Text } from 'react-native';
import {} from '../../api/endpoints';

export default function Clases() {
  return (
    <View className="flex-1 items-center justify-center bg-gray-800">
      <Text className="text-white">Hello Clases Gym</Text>
    </View>
  );
}
